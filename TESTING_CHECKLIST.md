# 🧪 H5 Meimo Demo — 人工 UAT 测试清单
**版本**: v1.0  
**适用环境**: localhost / 预生产 / 生产  
**预估耗时**: 2-3 小时（首次全量回归）  
**测试原则**: 每条用例都标了 [P0/P1/P2] 优先级，P0 = 必须全绿才能上线

---

## 📋 测试前准备 (Pre-flight)

| # | 检查项 | 通过条件 |
|---|--------|---------|
| 0.1 | `npm run dev` 启动无错 | 终端显示 `Ready` 且无 `Error`/`ECONNREFUSED` |
| 0.2 | 浏览器控制台无红色错误 | 仅允许 warn/调试日志 |
| 0.3 | Redis 连接成功 | `terminals/557298.txt` 显示 `[READY] REDIS` |
| 0.4 | PostgreSQL 连接成功 | `/api/admin/stats` 返回 200 |
| 0.5 | 战斗资源加载 | `/battle` 页面 5s 内进入正常画面 |

---

## 1️⃣ 首页 (Home /) — Banner 轮播

| # | 用例 | 优先级 | 步骤 | 通过条件 |
|---|------|--------|------|---------|
| H-01 | 无活动时首页 | [P0] | DB 里无 active 活动，访问 `/` | 不显示假倒计时，轮播区为空（TC-BN-12） |
| H-02 | 单个 Banner | [P0] | 创建 1 个 active banner | 轮播不自动播放（仅 1 个时关闭轮播） |
| H-03 | 多个 Banner 自动轮播 | [P0] | 创建 ≥2 个 active banner | 5 秒间隔自动切换（可配） |
| H-04 | 全局开关 | [P0] | Admin → Config 关闭 `isGlobalEnabled` | 首页轮播立即消失（30s 内/或刷新） |
| H-05 | 已结束 Banner 过滤 | [P0] | 创建 endTime < now 的 banner | 不显示在轮播中（TC-BN-09） |
| H-06 | 未开始 Banner | [P1] | 创建 startTime > now 的 banner | 状态 = `upcoming`，可显示但不激活 |
| H-07 | Banner 点击跳转 | [P0] | 点击 Banner | 跳到对应 `linkUrl`，无 404 |
| H-08 | 奖励未领取红点 | [P1] | 任意 Banner 有 `hasRewardUnclaimed=true` | TabBar/角标显示红点（**注：当前硬编码为 true，需验证是预期行为**） |
| H-09 | 首屏加载时间 | [P1] | 弱网 3G 模拟 | LCP < 2.5s |

---

## 2️⃣ 战斗页 (/battle) — 核心玩法

### 2.1 加载与资源

| # | 用例 | 优先级 | 通过条件 |
|---|------|--------|---------|
| B-01 | 首进加载 | [P0] | LoadingScreen 进度 0→100%，Stage1+2 资源加载完成后进入战斗 |
| B-02 | 二次进入（缓存） | [P0] | 极简同步 UI（TC-LD-05），无黑屏闪烁 |
| B-03 | 强制重新加载 | [P1] | URL 加 `?force_load=1` | 重新走完整 LoadingScreen |
| B-04 | Spine Stage 切换 | [P0] | HP 降到 75% → Stage2 形态变化 | Spine 模型切换，攻击动作连贯 |
| B-05 | Form 解锁 | [P0] | HP 50% 时 `awakening` 表解锁 | FormSelector 显示可点击 |
| B-06 | Form 锁定 | [P0] | HP 100% 时 `awakening` 锁 | FormSelector 显示锁图标，不可点 |

### 2.2 攻击系统

| # | 用例 | 优先级 | 通过条件 |
|---|------|--------|---------|
| B-10 | 道具 A 攻击 | [P0] | 点击"道具A"，出现伤害飘字、Boss 扣血 |
| B-11 | 道具 B 攻击 | [P0] | 同上，伤害更大（propB > propA） |
| B-12 | 攻击节流（200ms） | [P0] | 连点按钮 | 按钮立即禁用，直到动画完成才恢复（TC-BT-09） |
| B-13 | 攻击超时 | [P1] | 断网后点击攻击 | 5 秒后弹"网络超时" toast，扣血回滚（TC-BT-08） |
| B-14 | 幂等性 | [P0] | 同一 nonce 重发 | 返回 409 + `DUPLICATE_ATTACK`，不重复扣血 |
| B-15 | 道具耗尽 | [P0] | 道具数=0 时点击 | 弹"物品不足" toast，跳转到任务页 |
| B-16 | 伤害飘字视觉 | [P1] | 飘字位置/颜色/字号 | 道具 A 金色、道具 B 蓝色，无重叠错位 |
| B-17 | 粒子效果 | [P1] | 攻击时粒子喷射 | 性能 ≥ 50 FPS（DevTools Performance 录制） |
| B-18 | 累计伤害显示 | [P0] | 多次攻击后 | 顶部"累计伤害"数字实时更新 |
| B-19 | HP 实时同步 | [P0] | Admin 调 HP 后 3s 内 | H5 端 HP 条自动同步（TC-BT-05） |

### 2.3 形态切换

| # | 用例 | 优先级 | 通过条件 |
|---|------|--------|---------|
| B-20 | 切换至 Stage2 | [P0] | HP 跌破 75% | Spine 播放 morph 动画，无卡死 |
| B-21 | 切回 Stage1 | [P1] | Admin 重置 HP 满血 | 模型完全重置，无"冻结态"残留（A1/A2 修复） |
| B-22 | 攻击中切换形态 | [P1] | 攻击动画期间切换 | 攻击锁立即释放（TC-BT-09） |

### 2.4 音频

| # | 用例 | 优先级 | 通过条件 |
|---|------|--------|---------|
| B-30 | BGM 自动播放 | [P0] | 进入战斗 | 阶段对应 BGM 响起 |
| B-31 | AudioContext 唤醒 | [P0] | Edge 浏览器首次点击 | BGM 立即响（无 autoplay-block 静默） |
| B-32 | 待机语音 | [P1] | 点击 Spine 角色 | 播放当前阶段 standby 语音，攻击按钮临时禁用 |
| B-33 | 音量控制 | [P0] | 点击音量按钮 → 拖动滑块 | BGM 实时调节，记忆到 localStorage |
| B-34 | 静音持久化 | [P1] | 静音后刷新页面 | 仍为静音状态 |

### 2.5 模态面板

| # | 用例 | 优先级 | 通过条件 |
|---|------|--------|---------|
| B-40 | 每日任务面板 | [P0] | 点击"每日任务" | 模态弹出，显示 2 个任务进度 |
| B-41 | 进度奖励面板 | [P0] | 点击"进度奖励" | 模态弹出，显示里程碑进度 |
| B-42 | 活动规则 | [P1] | 点击规则图标 | 显示当前活动规则文本 |
| B-43 | 排行榜 | [P1] | 点击排行榜 | 显示 Top 10 玩家 |
| B-44 | 模态防重入 | [P0] | 快速点击入口 5 次 | 只打开 1 个模态（modalTriggerLock 250ms 防抖） |
| B-45 | 模态外点击关闭 | [P1] | 点击模态外区域 | 模态关闭 |
| B-46 | 模态中攻击锁定 | [P0] | 打开模态后点攻击 | 攻击按钮被忽略（TC-BT-04） |

### 2.6 活动状态门控

| # | 用例 | 优先级 | 通过条件 |
|---|------|--------|---------|
| B-50 | 活动未开始 | [P0] | startTime 未到 | 显示 "活动尚未开始" 全屏遮罩 |
| B-51 | 活动中 | [P0] | 在 startTime~endTime 之间 | 正常战斗 |
| B-52 | 活动结束 | [P0] | endTime 过后 | 显示 ActivityEndPage，攻击按钮禁用 |
| B-53 | 服务端时间校准 | [P1] | 客户端篡改系统时间 | 使用 `/api/time` 校准，不被欺骗 |
| B-54 | 资源加载失败 | [P0] | Spine 加载失败 | 显示 ERROR 屏，可点"强制进入" |

---

## 3️⃣ Admin 后台 (/admin)

### 3.1 登录

| # | 用例 | 优先级 | 通过条件 |
|---|------|--------|---------|
| A-01 | 错误密码 | [P0] | 输入错密码 | 401，错误次数记录 |
| A-02 | 5 次错误限流 | [P0] | 连续 5 次错误 | 第 6 次返回 429 + `Retry-After` |
| A-03 | 成功登录 | [P0] | 输入正确密码 | 下发 HttpOnly cookie，跳转 `/admin` |
| A-04 | 空密码 | [P1] | 提交空字符串 | 400 "密码不能为空" |
| A-05 | 篡改 cookie | [P0] | 手动改 `admin_token` 值为垃圾 | 重定向到 `/admin/login` |
| A-06 | 过期 cookie | [P1] | 等 24h 后访问 | 重定向到 `/admin/login` |
| A-07 | Dev bypass | [P1] | dev 模式下密码 = "dev" | 可登录（仅 dev） |

### 3.2 Dashboard

| # | 用例 | 优先级 | 通过条件 |
|---|------|--------|---------|
| A-10 | 数据加载 | [P0] | 访问 `/admin` | 6 张卡片显示真实数字，无 ⚠️ 错误条 |
| A-11 | 自动刷新 | [P1] | 等 30s | 数字自动更新（不需手动 reload） |
| A-12 | 数据失败提示 | [P1] | 停掉 Redis | 顶部红色 banner 显示具体错误 |
| A-13 | 快捷入口 | [P0] | 点击 3 个快捷入口卡片 | 正确跳转 |
| A-14 | 游戏信息 | [P1] | 有/无活动时 | 有则显示完整信息，无则显示"暂无进行中活动" |

### 3.3 活动管理

| # | 用例 | 优先级 | 通过条件 |
|---|------|--------|---------|
| A-20 | 创建活动 | [P0] | 填表 → 提交 | 新活动出现在列表 |
| A-21 | 时间冲突 | [P1] | 时间窗口重叠 | 警告提示或拒绝 |
| A-22 | 编辑活动 | [P0] | 修改已有活动 | H5 端 ≤ 30s 内反映 |
| A-23 | 设为 active | [P0] | Set Active | H5 端 `activityEnabled=true` |
| A-24 | 结束活动 | [P0] | Finalize | 触发 ActivityEndPage |
| A-25 | 活动配置页 | [P1] | 进入 `[id]/config` | 修改伤害权重、道具阈值 |

### 3.4 用户管理

| # | 用例 | 优先级 | 通过条件 |
|---|------|--------|---------|
| A-30 | 搜索用户 | [P0] | 输入 uid / partial | 列表过滤正确 |
| A-31 | 补偿道具 | [P0] | 给某用户 +10 道具 A | 该用户 H5 端 ≤ 10s 内看到 |
| A-32 | 强制解锁形态 | [P1] | 解锁所有形态 | 该用户所有 Form 立即可用 |
| A-33 | 重置用户 | [P0] | Reset | 道具归零、攻击清零 |
| A-34 | 导出用户 | [P1] | Export | CSV 文件下载，字段完整 |
| A-35 | 库存查询 | [P1] | 查询 user_inventory | 显示当前道具数 |

### 3.5 Banner 管理

| # | 用例 | 优先级 | 通过条件 |
|---|------|--------|---------|
| A-40 | 新建 Banner | [P0] | 填表 → 上传图 → 提交 | 出现在 H5 端轮播 |
| A-41 | 编辑 Banner | [P0] | 修改 | H5 端 ≤ 30s 反映 |
| A-42 | 启用/停用 | [P0] | 切换 enabled | H5 端立即生效 |
| A-43 | 删除 Banner | [P0] | 删除 | 立即从 H5 消失 |

### 3.6 实时监控

| # | 用例 | 优先级 | 通过条件 |
|---|------|--------|---------|
| A-50 | Redis 状态 | [P0] | 监控页 | 显示连接状态、内存使用、key 数 |
| A-51 | PG 状态 | [P0] | 监控页 | 显示连接池、表行数 |
| A-52 | 实时流量 | [P1] | 监控页 | 攻击/秒图表实时滚动 |

---

## 4️⃣ API 安全 & 边界

| # | 用例 | 优先级 | 通过条件 |
|---|------|--------|---------|
| S-01 | Admin API 未授权 | [P0] | 无 cookie 请求 `/api/admin/users` | 401（Iron Gate 拦截） |
| S-02 | Battle API 未授权 | [P1] | 无 uid cookie 调 `/api/action/attack` | 401 |
| S-03 | 错误 nonce 类型 | [P1] | 发送 `{nonce: 123}` | 400 `BAD_REQUEST` |
| S-04 | 超长 nonce | [P1] | nonce 长度 > 64 | 400 |
| S-05 | SQL 注入 | [P0] | uid = `' OR 1=1--` | 参数化查询，无注入 |
| S-06 | 攻击 Lua 原子性 | [P0] | 并发 100 次攻击 | 总伤害 = 100 × 单次，不多不少 |
| S-07 | 攻击限流 | [P0] | 1s 内 50 次攻击 | 部分返回 429 `RATE_LIMITED` |
| S-08 | CORS | [P1] | 跨域请求 | 仅同源可调，跨域拒绝 |
| S-09 | 错误堆栈泄露 | [P0] | 触发 500 | 响应不包含 stack trace |
| S-10 | 文件上传校验 | [P0] | 上传 `.exe`/`.php` | 拒绝，仅允许图/视频 |
| S-11 | 内部 API 隔离 | [P0] | 外部访问 `/api/internal/*` | 401/IP 白名单 |

---

## 5️⃣ 性能 & 体验

| # | 用例 | 优先级 | 通过条件 |
|---|------|--------|---------|
| P-01 | 首页 LCP | [P0] | 3G 模拟 | < 2.5s |
| P-02 | 战斗页 TTI | [P0] | 首进战斗页 | < 3s（含资源） |
| P-03 | 攻击响应延迟 | [P0] | 点击到飘字出现 | < 200ms |
| P-04 | 帧率 | [P0] | 战斗中 | ≥ 50 FPS（中端手机） |
| P-05 | 内存泄漏 | [P0] | 战斗 10 分钟后 | 内存无单调增长（DevTools Memory 录制） |
| P-06 | 后台标签恢复 | [P1] | 切到后台 30s 后回前台 | 状态正确，无错位（visibilitychange 触发同步） |

---

## 6️⃣ 兼容性

| # | 平台 | 版本 |
|---|------|------|
| C-01 | iOS Safari | 16+ |
| C-02 | Android Chrome | 最新 |
| C-03 | Desktop Chrome | 最新 |
| C-04 | Desktop Edge | 最新 |
| C-05 | 微信内置浏览器 | iOS/Android |

每个平台至少跑通：登录 → 进战斗 → 攻击 → 看 HP 变化 → 退出

---

## ✅ 通过标准 (Exit Criteria)

满足以下全部条件方可上线：
- [ ] P0 用例 100% 通过
- [ ] P1 用例 ≥ 90% 通过（剩余记为已知问题）
- [ ] 无 P0 级别的安全漏洞
- [ ] 无内存泄漏
- [ ] 帧率稳定 ≥ 50 FPS
- [ ] 至少在 2 个浏览器 + 1 部真机上跑通

---

## 🐛 问题登记表模板

| 编号 | 用例 ID | 现象 | 截图 | 复现步骤 | 严重度 | 负责人 | 状态 |
|------|---------|------|------|---------|--------|--------|------|
| 1 | B-13 | ... | `screenshots/bug1.png` | 1. ... 2. ... | P0 | @xxx | Open |