# 💬 客户验收话术（回复模板）

> **场景**：客户在群里问 "**现在怎么样了？我们还有其他的东西需要交付吗？**"
> **用途**：可直接复制粘贴到客户群 / 微信群。

---

## ✅ 主回复（直接发送）

```
老板您好，部署已经全部就绪啦 🎉

📡 访问地址：http://98.93.252.250/  （直接打开这个就行，浏览器不要加端口号）

现在可以正式验收了，我们整理了一份验收测试清单，您按里面的步骤走一遍就行：
👉 文件：[CLIENT_ACCEPTANCE_GUIDE.md]

里面覆盖了所有功能模块：
✅ 首页 7 阶段加载
✅ Boss 战斗（Spine 动画 + 攻击扣血）
✅ 任务 / 奖励 / 规则 4 大 Tab
✅ Admin 后台登录
✅ 移动端响应式

有问题随时在群里 @ 我，看到立即响应 👍
```

---

## 🔧 配套补充话术（按场景选用）

### 场景 1：客户说"页面打开白屏了"

```
老板，刚那个白屏问题我们已经定位修复了 ✅
原因是浏览器缓存了旧的 Content Security Policy 配置，
现在服务器已经更新到最新版本了，您再 **Ctrl+F5 强制刷新** 一下就好。
如果还不行，截图发我，我看下。
```

### 场景 2：客户问"还有什么要交付的吗？"

```
老板，本期所有交付内容已经全部上线 ✅，包括：

📦 本次已交付：
1. H5 活动页面（前端 + 后端 API）
2. Admin 管理后台
3. Boss 战斗系统（含 Redis 高并发防刷）
4. 任务系统 / 奖励系统 / Banner 管理
5. 音效系统（含静音状态持久化）
6. AWS 服务器全套配置（EC2 + nginx + PM2 + UFW）

🤝 还需要贵方配合对接的（不在本次交付范围）：
1. **主站用户登录对接** — 需要您主站后端给个 Cookie 鉴权方式
2. **充值金额 Webhook 接口** — 需要您主站推用户充值数据给我们
3. **消耗精力回调接口** — 需要您主站推用户消耗数据给我们
4. **HTTPS 证书** — 待您确认正式域名后我们一起配上

这些对接项拉个群，我们后端跟您后端的兄弟对一下接口文档即可，半天到 1 天就能完成。
```

### 场景 3：客户反馈"打不开 / 报错"

```
老板麻烦您帮我看下：

1. 浏览器地址栏是不是写的：http://98.93.252.250/  （不要加 :3000）
2. **按 F12 打开控制台**，切换到 Console 标签
3. **截图发我**，我看下是什么报错
4. 同时把 Network 标签的截图也发我一下

一般 90% 的问题截图一看就知道原因 👍
```

### 场景 4：客户说"我没看到战斗页"

```
老板，战斗页需要先模拟登录态（主站对接前的临时方案）：

1. 浏览器先打开 http://98.93.252.250/
2. 等加载动画结束（5-10 秒）
3. **按 F12 打开开发者工具**
4. 切换到 Console 标签
5. 粘贴这段回车：
   document.cookie = "uid=11111111-1111-1111-1111-111111111111; path=/";
   location.href = "/battle";
6. 页面会自动跳到战斗页 ✅

或者更简单：用管理员账号密码登录到 /admin 后台也可以看到战斗页。
```

---

## 🛠 场景 6：客户说"战斗页进去报错 / 只看到错误页 / 模型出不来"

**使用条件**：客户在 `/battle` 页面报告：
- 整个战斗页变成红色 "ACTIVITY ERROR"
- 看到 `PixiJS unsafe-eval` 报错
- Google 字体加载失败的样式错误
- Boss 模型没出来

**最新修复版本已上线**（2026-07-12 18:00 UTC）：
- ✅ CSP 已放开 `unsafe-eval`（PixiJS v8 shader 编译需要）
- ✅ CSP 已放开 Google Fonts 主机
- ✅ 兜底页改为**非阻塞小提示**，不再整页替换

```
老板您好，战斗页的报错问题我们已经全面修复了，麻烦您：

1. **Ctrl+F5 强制刷新一下**（重要，要清掉浏览器缓存）
2. 重新设置 cookie 进入战斗页（路径 B）
3. 现在应该能看到：
   • 顶部 4 个 Tab 菜单 ✅
   • Boss HP 血条 ✅
   • 底部动作栏 ✅
4. 即使模型加载失败，您也会看到：
   • 一张剪影占位图（Boss 位置）
   • 顶部一个红色小提示条 "RESOURCE FALLBACK"
   • 其他战斗页 UI 都还在，不会再全屏替换为错误页

如果还有问题，麻烦把 F12 Console 截图发我看下 🙏
```

**配套内部排查话术**（如果上面不管用）：

```
老板您看下 F12 → Network 标签，
能不能看到有红色（失败）的请求？
特别是 .atlas .json .png 这些文件的，
把那几个失败的 URL 截图发我就行 👍
```

---

## 📊 修复变更说明（如果客户问"你们修了什么"）

```
老板，简单说一下我们修了 3 个地方：

1. CSP 安全策略：放行了 PixiJS 引擎需要的 eval 和 Google 字体域名
2. 角色模型加载：之前因为 CSP 严格模式挂掉了，现在可以正常加载 Spine 动画
3. 失败兜底页：即使模型加载失败，战斗页 UI 也会保留，
   不会出现之前那种"全屏错误页替换战斗页"的情况
```

---

End of 话术模板.

## 📎 配套发客户的内容

发送话术时，**同时附上**：

1. 📄 **验收测试指南**：[CLIENT_ACCEPTANCE_GUIDE.md](CLIENT_ACCEPTANCE_GUIDE.md)
2. 🌐 **访问地址**：`http://98.93.252.250/`

---

## ⚠️ 注意事项

| 雷区 | 应对 |
|------|------|
| 不要在群里发密码 | Admin 密码**单独私聊**发送 |
| 不要主动说服务器 IP 之外的运维信息 | IP / 服务器配置等只在内部沟通 |
| 不要承诺主站对接能立刻完成 | 明确说"需要贵方后端配合，半天到 1 天" |
| 客户截图报错时**先看图再回复** | 不要让客户重新描述 |

---

End of 话术模板.